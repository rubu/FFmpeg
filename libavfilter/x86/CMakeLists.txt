if (${CONFIG_SCENE_SAD})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/scene_sad_init.c)
endif ()

if (${CONFIG_AFIR_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/af_afir_init.c)
endif ()
if (${CONFIG_ANLMDN_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/af_anlmdn_init.c)
endif ()
if (${CONFIG_ATADENOISE_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_atadenoise_init.c)
endif ()
if (${CONFIG_BLEND_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_blend_init.c)
endif () 
if (${CONFIG_BWDIF_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_bwdif_init.c)
endif () 
if (${CONFIG_COLORSPACE_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/colorspacedsp_init.c)
endif () 
if (${CONFIG_CONVOLUTION_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_convolution_init.c)
endif ()
if (${CONFIG_EQ_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_eq_init.c)
endif ()
if (${CONFIG_FRAMERATE_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_framerate_init.c)
endif () 
if (${CONFIG_FSPP_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_fspp_init.c)
endif () 
if (${CONFIG_GBLUR_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_gblur_init.c)
endif () 
if (${CONFIG_GRADFUN_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_gradfun_init.c)
endif () 
if (${CONFIG_HFLIP_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_hflip_init.c)
endif () 
if (${CONFIG_HQDN3D_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_hqdn3d_init.c)
endif () 
if (${CONFIG_IDET_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_idet_init.c)
endif () 
if (${CONFIG_INTERLACE_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_interlace.asm)
endif () 
if (${CONFIG_LIMITER_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_limiter_init.c)
endif () 
if (${CONFIG_MASKEDCLAMP_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_maskedclamp_init.c)
endif () 
if (${CONFIG_MASKEDMERGE_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_maskedmerge_init.c)
endif () 
if (${CONFIG_NOISE_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_noise.c)
endif ()
if (${CONFIG_OVERLAY_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_overlay_init.c)
endif () 
if (${CONFIG_PP7_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_pp7_init.c)
endif () 
if (${CONFIG_PSNR_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_psnr_init.c)
endif () 
if (${CONFIG_PULLUP_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_pullup_init.c)
endif () 
if (${CONFIG_REMOVEGRAIN_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_removegrain_init.c)
endif () 
if (${CONFIG_SHOWCQT_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/avf_showcqt_init.c)
endif () 
if (${CONFIG_SPP_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_spp.c)
endif () 
if (${CONFIG_SSIM_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_ssim_init.c)
endif () 
if (${CONFIG_STEREO3D_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_stereo3d_init.c)
endif () 
if (${CONFIG_TBLEND_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_blend_init.c)
endif () 
if (${CONFIG_THRESHOLD_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_threshold_init.c)
endif () 
if (${CONFIG_TINTERLACE_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_tinterlace_init.c)
endif () 
if (${CONFIG_TRANSPOSE_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_transpose_init.c)
endif ()
if (${CONFIG_VOLUME_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/af_volume_init.c)
endif ()
if (${CONFIG_V360_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_v360_init.c)
endif ()
if (${CONFIG_W3FDIF_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_w3fdif_init.c)
endif () 
if (${CONFIG_YADIF_FILTER})
    list(APPEND AVFILTER_X86_SOURCE_FILES x86/vf_yadif_init.c)
endif ()
if (${HAVE_X86ASM})
    if (${CONFIG_SCENE_SAD})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/scene_sad.asm)
    endif ()

    if (${CONFIG_AFIR_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/af_afir.asm)
    endif ()
    if (${CONFIG_ANLMDN_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/af_anlmdn.asm)
    endif ()
    if (${CONFIG_ATADENOISE_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_atadenoise.asm)
    endif ()
    if (${CONFIG_BLEND_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_blend.asm)
    endif () 
    if (${CONFIG_BWDIF_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_bwdif.asm)
    endif () 
    if (${CONFIG_COLORSPACE_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/colorspacedsp.asm)
    endif ()
    if (${CONFIG_CONVOLUTION_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_convolution.asm)
    endif ()
    if (${CONFIG_EQ_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_eq.asm)
    endif ()
    if (${CONFIG_FRAMERATE_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_framerate.asm)
    endif () 
    if (${CONFIG_FSPP_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_fspp.asm)
    endif ()
    if (${CONFIG_GBLUR_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_gblur.asm)
    endif () 
    if (${CONFIG_GRADFUN_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_gradfun.asm)
    endif () 
    if (${CONFIG_HFLIP_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_hflip.asm)
    endif () 
    if (${CONFIG_HQDN3D_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_hqdn3d.asm)
    endif () 
    if (${CONFIG_IDET_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_idet.asm)
    endif () 
    if (${CONFIG_INTERLACE_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_interlace.asm)
    endif () 
    if (${CONFIG_LIMITER_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_limiter.asm)
    endif ()
    if (${CONFIG_MASKEDCLAMP_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_maskedclamp.asm)
    endif ()
    if (${CONFIG_MASKEDMERGE_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_maskedmerge.asm)
    endif ()
    if (${CONFIG_OVERLAY_FILTER})
    list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_overlay.asm)
    endif () 
    if (${CONFIG_PP7_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_pp7.asm)
    endif () 
    if (${CONFIG_PSNR_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_psnr.asm)
    endif () 
    if (${CONFIG_PULLUP_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_pullup.asm)
    endif () 
    if (${CONFIG_GPL})
        if (${CONFIG_REMOVEGRAIN_FILTER})
            list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_removegrain.asm)
        endif () 
    endif ()
    if (${CONFIG_SHOWCQT_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/avf_showcqt.asm)
    endif () 
    if (${CONFIG_SSIM_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_ssim.asm)
    endif () 
    if (${CONFIG_STEREO3D_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_stereo3d.asm)
    endif () 
    if (${CONFIG_TBLEND_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_blend.asm)
    endif () 
    if (${CONFIG_THRESHOLD_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_threshold.asm)
    endif () 
    if (${CONFIG_TINTERLACE_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_interlace.asm)
    endif ()
    if (${CONFIG_TRANSPOSE_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_transpose.asm)
    endif ()
    if (${CONFIG_VOLUME_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/af_volume.asm)
    endif () 
    if (${CONFIG_V360_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_v360.asm)
    endif () 
    if (${CONFIG_W3FDIF_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_w3fdif.asm)
    endif () 
    if (${CONFIG_YADIF_FILTER})
        list(APPEND AVFILTER_ASSEMBLY_SOURCE_FILES x86/vf_yadif.asm x86/yadif-16.asm x86/yadif-10.asm)
    endif () 
endif ()


set(AVFILTER_X86_SOURCES "${AVFILTER_X86_SOURCE_FILES}" PARENT_SCOPE)
set(AVFILTER_X86_ASSEMBLY_SOURCES "${AVFILTER_ASSEMBLY_SOURCE_FILES}" PARENT_SCOPE)